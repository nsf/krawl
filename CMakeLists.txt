CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(CRAWL)

SET(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${CMAKE_CURRENT_SOURCE_DIR}/cmake")

FIND_PACKAGE(LLVM 2.9 REQUIRED)

ADD_SUBDIRECTORY(compiler)
ADD_SUBDIRECTORY(ctocrawl)
